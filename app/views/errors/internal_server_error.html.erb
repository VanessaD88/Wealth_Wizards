<div class="container">
  <div class="row justify-content-center">
    <div class="col-12 col-md-10 col-lg-8">
      
      <!-- Error image display - image already contains error code -->
      <div class="error-image-container text-center">
        <%= image_tag "500.png", alt: "500 Error - Magic Failed", class: "error-image" %>
      </div>

      <!-- Error Card matching card-custom component styling -->
      <div class="card-custom">
        <div class="card-header text-center">
          <h2 class="mb-0">The Magic Failed!</h2>
        </div>
        <div class="card-body text-center">
          <p class="lead mb-3">
            Something went wrong on our end. Our Wealth Wizards are working to fix this issue.
          </p>
          <p class="mb-3">
            Don't worry - your progress is safe! This is a temporary problem that we're resolving.
          </p>
          
          <!-- Alert message for user reassurance -->
          <div class="error-alert text-start mb-4">
            <strong>What happened?</strong><br>
            The server encountered an unexpected error. This doesn't affect your saved data or progress.
          </div>
          
          <!-- Navigation links to help users find their way back -->
          <div class="error-links mb-4">
            <%= link_to "Return to Home", root_path %>
            <% if user_signed_in? %>
              <%= link_to "View Dashboard", dashboard_path %>
            <% end %>
            <%= link_to "Try Again", "#", onclick: "location.reload(); return false;" %>
          </div>

          <!-- Action buttons -->
          <div class="d-flex flex-wrap justify-content-center gap-2 mt-3">
            <%= link_to "Go to Home", root_path, class: "button-gold" %>
            <a href="#" onclick="location.reload(); return false;" class="button-gold">Reload Page</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

