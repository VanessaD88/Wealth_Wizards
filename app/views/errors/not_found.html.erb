<div class="container">
  <div class="row justify-content-center">
    <div class="col-12 col-md-10 col-lg-8">
      
      <!-- Error image display - image already contains error code -->
      <div class="error-image-container text-center">
        <%= image_tag "404.png", alt: "404 Error - Lost Wizard", class: "error-image" %>
      </div>

      <!-- Error Card matching card-custom component styling -->
      <div class="card-custom">
        <div class="card-header text-center">
          <h2 class="mb-0">Lost in the Financial Maze?</h2>
        </div>
        <div class="card-body text-center">
          <p class="lead mb-3">
            This page doesn't exist in our realm of Wealth Wizards.
          </p>
          <p class="mb-4">
            The page you're looking for may have been moved, deleted, or never existed. 
            Don't worry - even the wisest wizards get lost sometimes!
          </p>
          
          <!-- Navigation links to help users find their way back -->
          <div class="error-links mb-4">
            <%= link_to "Return to Home", root_path %>
            <% if user_signed_in? %>
              <%= link_to "View Dashboard", dashboard_path %>
              <%= link_to "Go to Gameboard", pages_gameboard_path %>
            <% end %>
          </div>

          <!-- Primary action button - conditional based on journey status -->
          <% if user_signed_in? %>
            <% if current_user.level && current_user.level.name != "Level 0" %>
              <%= link_to "Continue Journey", pages_gameboard_path, class: "button-gold mt-3" %>
            <% else %>
              <%= link_to "Start Your Journey", pages_gameboard_path, class: "button-gold mt-3" %>
            <% end %>
          <% else %>
            <%= link_to "Start Your Journey", new_user_session_path, class: "button-gold mt-3" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>

