<%# Levels Overview Page - Conditional view displayed when:
    - User has no level (level = 0 scenario)
    - Current level is completed
    Shows game instructions and available levels to start %>
<div class="container my-4">
  <%# Landing page header and instructions %>
  <h1>Become a Wealth Wizard!</h1>

  <section class="mb-4">
    <h2>Play. Learn. Level Up Your Money Skills.</h2>
    <p>
      Step into the world of Wealth Wizard. An adventure where saving, investing, and growing money is just a game. Complete challenges and make real-world money moves while having fun.
    </p>
    <p>
      Master Money like a Wizard and start your adventure in financial freedom right here.
    </p>
  </section>

  <%# Show current active level status if user is progressing through a level %>
  <% if @current_level && @current_level.completion_status == false %>
    <div class="alert alert-info mb-4">
      You're currently on: <strong><%= @current_level.name %></strong>. Continue on the gameboard.
    </div>
    <div class="mb-4">
      <%= link_to "Go to Gameboard", pages_gameboard_path, class: "btn btn-primary" %>
  <% end %>

  <%# Call-to-action: Only show Start Your Journey button when user can begin (no active/incomplete level) %>
  <% unless @current_level && @current_level.completion_status == false %>
    <div class="my-4 text-center">
      <%= button_to "Start Your Journey", levels_path, method: :post, class: "btn btn-lg btn-success" %>
    </div>
  <% end %>

  <%# Renders the levels selection as Bootstrap-style cards with class names handled by the app's theme %>
  <section>
    <h2>Choose Your Level</h2>
    <div class="row g-3">
      <% @levels.each do |lvl| %>
        <div class="col-12 col-md-6">
          <div class="card h-100">
            <div class="card-body">
              <%# Level name as card title - styled by app/theme CSS %>
              <h3 class="card-title"><%= lvl[:name] %></h3>
              <%# Level instructional description %>
              <p class="card-text"><%= lvl[:description] %></p>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </section>
</div>
